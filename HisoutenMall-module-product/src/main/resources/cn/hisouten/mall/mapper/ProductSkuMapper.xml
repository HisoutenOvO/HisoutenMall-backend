<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.hisouten.mall.mapper.ProductSkuMapper">
    <insert id="insertBatch">
        insert into product_sku (product_id, price, stock, specs, image, status)
        values
        <foreach collection="productSkuList" item="sku" separator=",">
            (#{sku.productId}, #{sku.price}, #{sku.stock}, #{sku.specs}, #{sku.image}, #{sku.status})
        </foreach>
    </insert>
    <select id="selectExistSkuList" resultType="cn.hisouten.mall.pojo.entity.ProductSku">
        select * from product_sku where product_id = #{productId} and deleted = 0
    </select>
</mapper>